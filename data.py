from collections import deaultDict
import socket, ip, sqlite3, numpy

def __init__(self, port):
    self.connections=defaultDict(int)

access=socket.socket(socket.AF_INET6, socket.SOCK_DGRAM)

async def require(data, port):
        pass

while True:
    access.bind(("", port))
    data, address= access.recvfrom(512 MSG_EOR)


