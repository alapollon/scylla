
todo 
def discover(*args):
    while args.get == ['target*'] 
        cap.conf
        ...

todo 
    q = netfilterqueue.NetfilterQueue()
    q.bind(0, process)
    q.run 


todo
logging.basicConfig(
    level=logging.DEBUG,
    format='(%(Appname)s %(threadName)-10s) %(message)s',
    filename="tacticalapp.log"
    stream= ,
)
todo
    for line 103 instruct the interpreter to print out the list of targets

todo
    import pandas as pd

# Create a MultiIndex
index = pd.MultiIndex.from_product([['A', 'B'], [1, 2, 3]], names=['Letter', 'Number'])
data = [10, 20, 30, 40, 50, 60]
df = pd.DataFrame(data, index=index, columns=['Value'])

print(df)

 self.node = pd.MultiIndex.from_product([[string_list_of_internet_services.filter()],
        [args.get['port_services'], args.get['host_ipv4_address'], args.get['host_ipv6_address'], args.get['domain_name']]], 
        names = ['port services', 'hostname']) 